[project]
name = "doge"
version = "1.0.0"
description = "Updates based on data provided by doge.gov API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4==4.15.0",
    "curl-cffi==0.15.0",
]

[dependency-groups]
dev = [
    "pre-commit==4.6.0",
]
