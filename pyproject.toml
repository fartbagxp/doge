[project]
name = "doge"
version = "1.0.0"
description = "Updates based on data provided by doge.gov API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4==4.14.3",
    "curl-cffi==0.14.0",
]

[dependency-groups]
dev = [
    "pre-commit==4.5.1",
]
